# Scope

- Build what the current stage uses. No speculative flag, no setting nothing reads, no infrastructure for a stage that hasn't started.
- An intermediate format is fine when it earns its place; say what it is for.
